import React, {ReactElement} from "react";
import './ReservationsSection.css'

export function ReservationsSection(): ReactElement {
    return <section className="reservations_section">
        <h2>Make a reservation</h2>
    </section>
}